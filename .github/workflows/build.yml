name: Build Custom Photon Server

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '7.0.x' # или версия .NET, совместимая с netstandard2.1

    - name: Restore NuGet packages
      run: dotnet restore RepoCustomPhotonServer.sln

    - name: Build solution
      run: dotnet build RepoCustomPhotonServer.sln --configuration Release

    - name: Publish output
      run: |
        mkdir output
        copy RepoCustomPhotonServer\bin\Release\netstandard2.1\* output\
    
    - name: Upload artifact
      uses: actions/upload-artifact@v3
      with:
        name: CustomPhotonServer
        path: output/
